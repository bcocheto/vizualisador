import React from "react";
import styles from "../styles/MainHeader.module.scss";

const MainHeader = () => {
  return (
    <header className={styles.header}>
      <h1>Vizualisador de Algoritmos de Inserção e Seleção</h1>
    </header>
  );
};

export default MainHeader;
